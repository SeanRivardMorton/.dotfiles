local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
	height = 40,
	-- color = colors.bar.bg,
	-- border_color = colors.bar.border,
	padding_right = 30,
	padding_left = 30,
	-- border_width = 2,
	-- corner_radius = 10,
	y_offset = 12,
})
